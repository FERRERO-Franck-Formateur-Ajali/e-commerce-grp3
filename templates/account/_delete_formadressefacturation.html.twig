<form method="post" action="{{ path('adressefacturation_delete', {'id': adressefacturation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adressefacturation.id) }}">
    <button class="btn">Delete</button>
</form>
