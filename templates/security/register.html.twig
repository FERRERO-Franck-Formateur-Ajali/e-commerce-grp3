{% extends 'base.html.twig' %}

{% block title %}Page Créer un compte{% endblock %}

{% block body %}
<div class="container">
	<div class="row">
            {{ form_start(formUser) }}
            <div class="col-sm-4 mx-auto">
            {{ form_widget(formUser) }}
            </div>
            {# {{ form_start(formClient) }}
            <div class="col-sm-4 mx-auto">
            {{ form_widget(formClient) }}
            </div> #}
            <button type="submit" class="btn btn-primary">enregistrer</button>
            {# {{ form_end(formClient) }} #}
             {{ form_end(formUser) }}
    </div>
</div>
{% endblock %}
